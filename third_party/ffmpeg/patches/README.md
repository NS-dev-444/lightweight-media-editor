# FFmpeg patches

Any patch placed here is applied by `build.sh` in filename order.

**LGPL obligation:** every patch in this directory must be published as a diff
alongside the shipped binaries and the pinned source tarball. Adding a patch is
a licensing-relevant change — re-run the audit in
`docs/DEPENDENCY_AND_LICENSE_AUDIT.md` before shipping.

Empty is the preferred state.
